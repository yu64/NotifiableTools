# yaml-language-server: $schema=./schema.json

Rules:
  - IntervalMilliseconds: 100
    Name: "ルールA"
    Enable: true
    Condition:
      $type: BoolSymbol
      Value: true
    Notions:
      - $type: Button
        Title: "実行"
        


